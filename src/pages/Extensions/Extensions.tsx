import { Card } from 'src/components/Card';
import { EnterAnimation } from '../../components/EnterAnimation/EnterAnimation';

export const Extensions = () => {
  return (
    <EnterAnimation>
      <Card>Extensions</Card>
    </EnterAnimation>
  );
};
